import React from "react";

// Placeholder — dashboard screen for the risk_assessment agent. Not wired into any route
// yet; mirrors backend/src/{router,controller,services}/agents/risk_assessment_agent/.
export function RiskAssessmentAgent() {
  return <div>RiskAssessmentAgent — dashboard placeholder</div>;
}
