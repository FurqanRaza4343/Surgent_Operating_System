import React from "react";

// Placeholder — dashboard screen for the recovery_dashboard agent. Not wired into any route
// yet; mirrors backend/src/{router,controller,services}/agents/recovery_dashboard_agent/.
export function RecoveryDashboardAgent() {
  return <div>RecoveryDashboardAgent — dashboard placeholder</div>;
}
