import React from "react";

// Placeholder — dashboard screen for the appointment_booking agent. Not wired into any route
// yet; mirrors backend/src/{router,controller,services}/agents/appointment_booking_agent/.
export function AppointmentBookingAgent() {
  return <div>AppointmentBookingAgent — dashboard placeholder</div>;
}
