import React from "react";

// Placeholder — dashboard screen for the surgery_scheduling agent. Not wired into any route
// yet; mirrors backend/src/{router,controller,services}/agents/surgery_scheduling_agent/.
export function SurgerySchedulingAgent() {
  return <div>SurgerySchedulingAgent — dashboard placeholder</div>;
}
