import React from "react";

// Placeholder — dashboard screen for the operating_room_scheduler agent. Not wired into any route
// yet; mirrors backend/src/{router,controller,services}/agents/operating_room_scheduler_agent/.
export function OperatingRoomSchedulerAgent() {
  return <div>OperatingRoomSchedulerAgent — dashboard placeholder</div>;
}
