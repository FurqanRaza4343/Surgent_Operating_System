import React from "react";

// Placeholder — dashboard screen for the cost_estimation agent. Not wired into any route
// yet; mirrors backend/src/{router,controller,services}/agents/cost_estimation_agent/.
export function CostEstimationAgent() {
  return <div>CostEstimationAgent — dashboard placeholder</div>;
}
