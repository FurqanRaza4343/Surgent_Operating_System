import React from "react";

// Placeholder — dashboard screen for the implant_inventory agent. Not wired into any route
// yet; mirrors backend/src/{router,controller,services}/agents/implant_inventory_agent/.
export function ImplantInventoryAgent() {
  return <div>ImplantInventoryAgent — dashboard placeholder</div>;
}
