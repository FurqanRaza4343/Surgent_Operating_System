import React from "react";

// Placeholder — dashboard screen for the pre_surgery_preparation agent. Not wired into any route
// yet; mirrors backend/src/{router,controller,services}/agents/pre_surgery_preparation_agent/.
export function PreSurgeryPreparationAgent() {
  return <div>PreSurgeryPreparationAgent — dashboard placeholder</div>;
}
