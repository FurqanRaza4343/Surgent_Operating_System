import React from "react";

// Placeholder — dashboard screen for the patient_feedback agent. Not wired into any route
// yet; mirrors backend/src/{router,controller,services}/agents/patient_feedback_agent/.
export function PatientFeedbackAgent() {
  return <div>PatientFeedbackAgent — dashboard placeholder</div>;
}
