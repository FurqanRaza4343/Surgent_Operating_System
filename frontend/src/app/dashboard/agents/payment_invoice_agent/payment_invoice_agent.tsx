import React from "react";

// Placeholder — dashboard screen for the payment_invoice agent. Not wired into any route
// yet; mirrors backend/src/{router,controller,services}/agents/payment_invoice_agent/.
export function PaymentInvoiceAgent() {
  return <div>PaymentInvoiceAgent — dashboard placeholder</div>;
}
