import React from "react";

// Placeholder — dashboard screen for the photo_analysis agent. Not wired into any route
// yet; mirrors backend/src/{router,controller,services}/agents/photo_analysis_agent/.
export function PhotoAnalysisAgent() {
  return <div>PhotoAnalysisAgent — dashboard placeholder</div>;
}
