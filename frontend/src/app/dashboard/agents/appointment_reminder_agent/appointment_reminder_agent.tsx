import React from "react";

// Placeholder — dashboard screen for the appointment_reminder agent. Not wired into any route
// yet; mirrors backend/src/{router,controller,services}/agents/appointment_reminder_agent/.
export function AppointmentReminderAgent() {
  return <div>AppointmentReminderAgent — dashboard placeholder</div>;
}
