import React from "react";

// Placeholder — dashboard screen for the reschedule_cancellation agent. Not wired into any route
// yet; mirrors backend/src/{router,controller,services}/agents/reschedule_cancellation_agent/.
export function RescheduleCancellationAgent() {
  return <div>RescheduleCancellationAgent — dashboard placeholder</div>;
}
