import React from "react";

// Placeholder — dashboard screen for the recovery_followup agent. Not wired into any route
// yet; mirrors backend/src/{router,controller,services}/agents/recovery_followup_agent/.
export function RecoveryFollowupAgent() {
  return <div>RecoveryFollowupAgent — dashboard placeholder</div>;
}
