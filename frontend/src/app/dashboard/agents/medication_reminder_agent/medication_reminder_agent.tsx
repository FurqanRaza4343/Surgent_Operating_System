import React from "react";

// Placeholder — dashboard screen for the medication_reminder agent. Not wired into any route
// yet; mirrors backend/src/{router,controller,services}/agents/medication_reminder_agent/.
export function MedicationReminderAgent() {
  return <div>MedicationReminderAgent — dashboard placeholder</div>;
}
