import React from "react";

// Placeholder — dashboard screen for the video_consultation agent. Not wired into any route
// yet; mirrors backend/src/{router,controller,services}/agents/video_consultation_agent/.
export function VideoConsultationAgent() {
  return <div>VideoConsultationAgent — dashboard placeholder</div>;
}
