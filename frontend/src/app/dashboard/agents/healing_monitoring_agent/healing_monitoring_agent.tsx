import React from "react";

// Placeholder — dashboard screen for the healing_monitoring agent. Not wired into any route
// yet; mirrors backend/src/{router,controller,services}/agents/healing_monitoring_agent/.
export function HealingMonitoringAgent() {
  return <div>HealingMonitoringAgent — dashboard placeholder</div>;
}
