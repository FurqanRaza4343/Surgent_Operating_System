import React from "react";

// Placeholder — dashboard screen for the ai_consultation agent. Not wired into any route
// yet; mirrors backend/src/{router,controller,services}/agents/ai_consultation_agent/.
export function AiConsultationAgent() {
  return <div>AiConsultationAgent — dashboard placeholder</div>;
}
