import React from "react";

// Placeholder — dashboard screen for the emergency_triage agent. Not wired into any route
// yet; mirrors backend/src/{router,controller,services}/agents/emergency_triage_agent/.
export function EmergencyTriageAgent() {
  return <div>EmergencyTriageAgent — dashboard placeholder</div>;
}
