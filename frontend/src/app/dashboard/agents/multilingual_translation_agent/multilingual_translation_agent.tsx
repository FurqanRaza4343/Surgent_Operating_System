import React from "react";

// Placeholder — dashboard screen for the multilingual_translation agent. Not wired into any route
// yet; mirrors backend/src/{router,controller,services}/agents/multilingual_translation_agent/.
export function MultilingualTranslationAgent() {
  return <div>MultilingualTranslationAgent — dashboard placeholder</div>;
}
