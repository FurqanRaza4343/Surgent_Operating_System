import React from "react";

// Placeholder — dashboard screen for the procedure_recommendation agent. Not wired into any route
// yet; mirrors backend/src/{router,controller,services}/agents/procedure_recommendation_agent/.
export function ProcedureRecommendationAgent() {
  return <div>ProcedureRecommendationAgent — dashboard placeholder</div>;
}
