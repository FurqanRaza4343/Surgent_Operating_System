import React from "react";

// Placeholder — dashboard screen for the surgeon_calendar agent. Not wired into any route
// yet; mirrors backend/src/{router,controller,services}/agents/surgeon_calendar_agent/.
export function SurgeonCalendarAgent() {
  return <div>SurgeonCalendarAgent — dashboard placeholder</div>;
}
