import React from "react";

// Placeholder — dashboard screen for the lead_nurturing agent. Not wired into any route
// yet; mirrors backend/src/{router,controller,services}/agents/lead_nurturing_agent/.
export function LeadNurturingAgent() {
  return <div>LeadNurturingAgent — dashboard placeholder</div>;
}
