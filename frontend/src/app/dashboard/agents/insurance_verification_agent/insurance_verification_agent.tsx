import React from "react";

// Placeholder — dashboard screen for the insurance_verification agent. Not wired into any route
// yet; mirrors backend/src/{router,controller,services}/agents/insurance_verification_agent/.
export function InsuranceVerificationAgent() {
  return <div>InsuranceVerificationAgent — dashboard placeholder</div>;
}
