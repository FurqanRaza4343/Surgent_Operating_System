import React from "react";

// Placeholder — dashboard screen for the equipment_checklist agent. Not wired into any route
// yet; mirrors backend/src/{router,controller,services}/agents/equipment_checklist_agent/.
export function EquipmentChecklistAgent() {
  return <div>EquipmentChecklistAgent — dashboard placeholder</div>;
}
