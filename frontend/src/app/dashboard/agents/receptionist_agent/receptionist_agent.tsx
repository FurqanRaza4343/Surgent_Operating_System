import React from "react";

// Placeholder — dashboard screen for the receptionist agent. Not wired into any route
// yet; mirrors backend/src/{router,controller,services}/agents/receptionist_agent/.
export function ReceptionistAgent() {
  return <div>ReceptionistAgent — dashboard placeholder</div>;
}
