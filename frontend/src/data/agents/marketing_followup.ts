import { MegaphoneIcon } from "lucide-react";
import type { Agent } from "./types";

export const marketingFollowup: Agent = {
  slug: "marketing_followup",
  name: "Marketing Follow-up",
  desc: "Re-engages every lead with the right message.",
  icon: MegaphoneIcon,
  categoryId: "business"
};
