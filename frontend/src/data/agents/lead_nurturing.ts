import { SproutIcon } from "lucide-react";
import type { Agent } from "./types";

export const leadNurturing: Agent = {
  slug: "lead_nurturing",
  name: "Lead Nurturing",
  desc: "Warms prospects into booked consultations.",
  icon: SproutIcon,
  categoryId: "business"
};
