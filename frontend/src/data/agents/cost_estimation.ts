import { DollarSignIcon } from "lucide-react";
import type { Agent } from "./types";

export const costEstimation: Agent = {
  slug: "cost_estimation",
  name: "Cost Estimation",
  desc: "Generates instant, accurate quotes.",
  icon: DollarSignIcon,
  categoryId: "business"
};
